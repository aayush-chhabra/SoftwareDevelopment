<h3></h3>
<div id="piechart_3d" style="width: 100%; height: 500px;"></div>

<script type="text/javascript">
    google.load("visualization", "1", { packages: ["corechart"] });
    google.setOnLoadCallback(drawChart);

    function drawChart() {
        //        //var requestData = {
        //        //    serviceId: "1"
        //        //};

        //        $.post({
        //            url: 'ServiceUsage',
        //            type: 'POST',
        ////            data: JSON.stringify(requestData),
        //            dataType: 'json',
        //            contentType: 'application/json; charset=utf-8',
        //            success: function (data) {
        //                var tdata = new google.visualization.DataTable();
        //                tdata.addColumn('string', 'Projects');
        //                tdata.addColumn('number', 'Amount Used');

        //                for (var i = 0; i < data.length; i++) {
        //                    tdata.addRow([data[i].Name, data[i].Value]);
        //                }

        //                var options = {
        //                    title: 'Detailed Service Usage by Projects',
        //                    is3D: true,
        //                };

        //                var chart = new google.visualization.PieChart(document.getElementById('piechart_3d'));
        //                chart.draw(data, options);
        //            },
        //            error: function (xhr) {
        //                alert('Error: ' + xhr.statusText);
        //            },
        //            async: true,
        //            processData: false
        //        });
        //    }

        $.post('../Reporting/ServiceUsage', {}, function (data) {
            var tdata = new google.visualization.DataTable();

            tdata.addColumn('string', 'Projects');
            tdata.addColumn('number', 'Amount Used');

            for (var i = 0; i < data.length; i++) {
                tdata.addRow([data[i].Name, data[i].Value]);
            }

            var options = {
                title: 'Detailed Service Usage by Projects: Statistical Advisory Service',
                is3D: true,
            };

            var chart = new google.visualization.PieChart(document.getElementById('piechart_3d'));
            chart.draw(tdata, options);
        });
    }
</script>
